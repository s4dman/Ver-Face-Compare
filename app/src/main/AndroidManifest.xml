<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sadmanhasan.ver_face_compare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VerFaceCompare">
        <activity android:name=".SessionResultActivity" />
        <activity android:name=".LivenessSessionActivity" />

        <meta-data
            android:name="com.appliedrec.verid.password"
            android:value="dac79733-91f1-4cdd-abb4-f4865cdfd704" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>